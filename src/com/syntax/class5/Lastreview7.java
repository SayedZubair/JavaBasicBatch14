package com.syntax.class5;

import java.util.Scanner;

public class Lastreview7 {

	public static void main(String[] args) {
		boolean programmer= true;
		boolean manualTester= true;
		boolean knowFramework=true;
		if (programmer) {
			if(manualTester) {
				if(knowFramework) {
			
			System.out.println("you can be an automation enginner ");
		}else {
			
			System.out.println("you must know a programming lframework");
		}
	}else {
		System.out.println("you need to know manual");
	}
		}else { System.out.println("you need to know a programer");
	}
		}
}
/////why we are not using logical operators in stead of nested if .. if the logicals are so good 
/// when you want differet output in each condition we use if ,, if we want to use operators we get one output 
	// the out if is reletaed to line 23
// we can have a print under each if 
// else is there if not happening 
////if else if condtions are used when we use different conditions with different print we,,,logical &&can be
//nested if 
//logical || we can replace if else if ... but we get only one print 
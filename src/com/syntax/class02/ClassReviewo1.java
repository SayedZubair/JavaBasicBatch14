package com.syntax.class02;

public class ClassReviewo1 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		/* 
		 * practicing the primitive data type 
		 * byte
		 * short 
		 * int
		 * long
		 * only for the whole numbers we cant use decimals in the above data types, we can use all the four types
		 * for the numbers but based on their size, int is the common one as it can store up to billions and does not
		 * waste a lot of space from RAM
		 * 
		 */
		int box1 = 12;
		// int -> date type 
		//box1 variable name
		// = to assign the data to the box 
		// ; language rule we need to end all the lines with semicolons.
		// we cant store a character in the boxes 
		//char box1 = 's'; // not allowed as makes confusion.
		
		int box2; // creating a box but not storing data right away in the box 
		box2=20;
		box1 = 20;  // replacing the value box1 from 12 to 20 
		
		System.out.println(box1);            
		System.out.println(box1 + box2 );
	}

}
